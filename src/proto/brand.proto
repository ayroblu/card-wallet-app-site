syntax = "proto3";
package card_wallet_app.brands;

message Brand {
    string id = 1;
    string name = 2;
    repeated string countries = 3;
    repeated string search_terms = 4;
    string colour = 5;
    int32 last_modified = 6;
    optional string url = 7;
}
message Brands { repeated Brand brands = 1; }
